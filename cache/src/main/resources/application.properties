
server.port=8080

# #  Actuator :- http://{host}:{port}/actuator     # #

management.security.enabled=false
management.endpoints.web.exposure.include=*

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

server.ssl.key-alias=SpringCacheApplication
server.ssl.key-store-type=JKS
server.ssl.key-password=ANEKSINGH
server.ssl.key-store=classpath:SpringCacheApplication.jks
#   Generate via Bash
# keytool -genkey -alias SpringCacheApplication -storetype JKS -keyalg RSA -keysize 1024 -keystore SpringCacheApplication.jks